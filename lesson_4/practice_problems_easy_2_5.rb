# class variables have the prefix `@@`
# instance variables have the prefix `@`
# local variables begin with either a letter or an underscore
# and contain only letters, underscores, digits

# So:

# `excited_dog` is a local variable
# `@excited_dog` is an instance variable
# `@@excited_dog` is a class variable
